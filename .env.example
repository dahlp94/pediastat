POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=pediastat
POSTGRES_USER=pediastat
POSTGRES_PASSWORD=
DATA_DIR=data

# Default local bootstrap uses a project-local cluster on port 5433:
#   python scripts/bootstrap_database.py --local-cluster
# If you already have PostgreSQL, set POSTGRES_PORT=5432 and a real password.
